
spring:
  application:
    name: Scrapper
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

rest:
  client:
    bot:
      base-url: http://localhost:8080
    github:
      token: token
      base-url: http://localhost:8080
    stack-exchange:
      base-url: http://localhost:8080
